id: section-body-offer-capacity
displayName: Offer capacity
version: 1.0.0
semanticRole: component-section-body.offer-capacity
description: |
  Section-body component that renders current offer-capacity state inside a
  section without owning the surrounding narrative or page routing.
expectedIntents:
  - render-offer-capacity
  - avoid-frozen-availability
expectedIndustryFit:
  - trades-and-construction
  - consulting-and-coaching
layoutHint: single-column
propsSchema:
  $shape: zod
  shape: |
    z.object({}).passthrough()
acceptedCosmicNames:
  - Thalassa
constraints: {}
