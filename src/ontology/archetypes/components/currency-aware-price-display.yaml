id: currency-aware-price-display
displayName: Currency Aware Price Display
version: 1.0.0
semanticRole: component-currency-aware-price-display
description: |
  Currency-aware price display with pre-rendered variants (RFC-0743).
  Pre-renders all currency variants server-side; client script toggles visibility.
expectedIntents:
  - pricing
  - trust
expectedIndustryFit: []
layoutHint: single-column
propsSchema:
  $shape: zod
  shape: |
    z.object({}).passthrough()
acceptedCosmicNames:
  - Rosalind
constraints: {}
