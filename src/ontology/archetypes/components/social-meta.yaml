id: social-meta
displayName: Social meta
version: 1.0.0
semanticRole: component-social-meta
description: |
  Starter RFC-0072 archetype seeded from existing manifest metadata (RFC-0178).
  Open Graph / Twitter Card meta injected alongside the page head (RFC-0162).
expectedIntents:
  - orient-visitor
  - fulfill-legal-requirement
expectedIndustryFit:
  - non-profit
layoutHint: split
propsSchema:
  $shape: zod
  shape: |
    z.object({}).passthrough()
acceptedCosmicNames:
  - Naiad
constraints: {}
