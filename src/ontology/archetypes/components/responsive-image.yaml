id: responsive-image
displayName: Responsive image
version: 1.0.0
semanticRole: component-responsive-image
description: |
  Starter RFC-0072 archetype seeded from existing manifest metadata (RFC-0178).
  Provider-agnostic responsive image (RFC-0152 Image Provider Port).
expectedIntents:
  - orient-visitor
expectedIndustryFit:
  - non-profit
layoutHint: split
propsSchema:
  $shape: zod
  shape: |
    z.object({}).passthrough()
acceptedCosmicNames:
  - Belinda
constraints: {}
