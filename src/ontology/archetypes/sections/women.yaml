id: women
displayName: Women
version: 1.2.0
semanticRole: women
description: |
  Image + paragraphs body. RFC-0103 bodyKind: paragraphs with section-level image.
expectedIntents:
  - humanise-brand
  - establish-identity
  - introduce-team
expectedIndustryFit:
  - trades-and-construction
  - non-profit
layoutHint: split
bodyKind: paragraphs
propsSchema:
  $shape: zod
  compose:
    - section-visual
    - section-header
    - body-paragraphs
  shape: |
    z.object({
      image: z.string().min(1),
      imageAlt: z.string().min(2),
    }).strict()
acceptedCosmicNames:
  - Iapetus
constraints: {}
