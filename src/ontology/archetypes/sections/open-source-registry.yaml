id: open-source-registry
displayName: Open Source Registry
version: 1.0.0
semanticRole: open-source-registry
description: |
  Deployment-specific SBOM registry section (RFC-0489). Renders a component table,
  summary counts, license distribution, deployment metadata, and download buttons
  from a structured JSON data file. Semantic HTML with accessibility attributes.
expectedIntents:
  - fulfill-legal-requirement
  - signal-transparency
expectedIndustryFit: []
layoutHint: single-column
bodyKind: composite
propsSchema:
  $shape: zod
  compose:
    - section-visual
  shape: |
    z.object({
      heading: z.string().optional(),
      dataRef: z.string().optional(),
    }).strict()
acceptedCosmicNames:
  - Orcus
constraints: {}
