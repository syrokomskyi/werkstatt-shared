id: trust-strip
displayName: Trust strip
version: 1.2.0
semanticRole: trust-evidence-row
description: |
  Horizontal row of 3-5 short trust signals. RFC-0103 bodyKind: list, no header.
expectedIntents:
  - trust
  - proof
expectedIndustryFit:
  - trades-and-construction
  - consulting-and-coaching
  - legal-services
  - non-profit
layoutHint: horizontal-row
bodyKind: list
recommendedItemCount:
  min: 3
  max: 5
propsSchema:
  $shape: zod
  compose:
    - section-visual
    - section-header
    - body-list
  shape: |
    z.object({
      cta: z.object({
        label: z.string().min(2),
        target: z.string().min(1),
      }).optional(),
    }).strict()
acceptedCosmicNames:
  - Deimos
constraints:
  forbidPhrases:
    - günstig
    - garantiert
    - Bestseller
  forbidIconLibraryTags:
    - stock-handshake
    - stock-thumbs-up
