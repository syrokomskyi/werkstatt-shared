id: notausgang-block
displayName: Notausgang block
version: 1.2.0
semanticRole: exit-clause-disclosure
description: |
  Explicit exit terms. RFC-0103 bodyKind: list; default tone warning.
expectedIntents:
  - trust
  - legal-clarity
  - de-risk
expectedIndustryFit:
  - trades-and-construction
  - consulting-and-coaching
  - legal-services
layoutHint: split
bodyKind: list
recommendedItemCount:
  min: 3
  max: 6
propsSchema:
  $shape: zod
  compose:
    - section-visual
    - section-header
    - body-list
  shape: |
    z.object({
      cta: z.object({
        label: z.string().min(2),
        target: z.string().min(1),
      }).optional(),
    }).strict()
acceptedCosmicNames:
  - Telesto
constraints:
  forbidPhrases:
    - Evakuierung
    - Notfall
    - Strafgebühr
  mandatoryConcepts:
    - Notausgang
